---
applyTo:
  - "**/*.cs"
---

# C# file guidance

- Keep the existing minimal style unless a change requires a broader refactor.
- Prefer straightforward console and object-model code over additional abstractions.
- Respect nullable reference types and `required` members when adding new properties.
- Avoid introducing new dependencies unless the task explicitly requires them.

